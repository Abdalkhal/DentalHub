Update the `/supplies` directory page (referenced in image 31193.jpg) to implement dual-filtering by Governorate and Sub-Category (All, Implants Companies, Supplies Companies), while gracefully handling legacy accounts.

### 1. Sub-Category Tabs (فئات الشركات):
- Add a category tab selector bar right under the governorates scrollbar with 3 options:
  - **الكل** (All) -> Displays both Implants and Supplies companies.
  - **شركات الزرعات** (Implant Companies) -> Filters accounts with category `implants` or `dental_implants`.
  - **شركات المستلزمات** (Medical Supplies Companies) -> Filters accounts with category `supplies` or `medical_supplies`.

### 2. Dual-Filtering Logic (`city` + `category`):
- Maintain two active filter states: `selectedCity` (defaults to "كل المدن") and `selectedCategory` (defaults to "الكل").
- Filtering logic rules:
  1. If `selectedCity === "كل المدن"`, show all companies matching the selected category filter regardless of city.
  2. If a specific governorate is selected (e.g., "نينوى"), only show companies where `account.city === "نينوى"` AND matches the `selectedCategory`.
  3. Clicking "شركات الزرعات" while "نينوى" is selected must ONLY display implant companies registered in Nineveh.

### 3. Legacy Accounts Handling (Default Governorate):
- Define a default fallback constant: `DEFAULT_CITY = "بغداد"`.
- When fetching or mapping companies/users from Firestore:
  - If `account.city` is missing, `null`, or `undefined`, assign `account.city = "بغداد"` in the client state.
  - Automatically update the document in Firestore in the background by setting `city: "بغداد"` so that legacy accounts permanently receive this default field without losing data.