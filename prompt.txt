Re-implement the navigation buttons and routes for searching Dentists, Medical Supplies Stores, and Dental Labs/Accounts in the main sidebar based on `31176.jpg`:

### Requirements:

1. **Update Sidebar Navigation Items (`src/components/MobileShell.tsx` or Sidebar layout component):**
   - Add explicit navigation links under the "القائمة" section for:
     - **أطباء الأسنان** (`/doctors`) -> Icon: UserCheck / Users -> Label: "أطباء الأسنان"
     - **المكاتب والمستلزمات** (`/supplies`) -> Icon: Store / Package -> Label: "المكاتب والمستلزمات"
     - **المختبرات والحسابات** (`/labs`) -> Icon: Building / Search -> Label: "البحث عن مختبرات/حسابات"

2. **Route Setup & Search/Filter Functionality:**
   - **For `/doctors`:** Display a searchable directory/list of registered dentists with filters by city/governorate and specialization.
   - **For `/supplies`:** Display a search/catalog page for dental supply offices, showing available products, offers, and store details.
   - **For `/labs`:** Display a directory page allowing users to search and discover other dental labs/accounts registered on DentalHub.

3. **UI Consistency:**
   - Use standard Lucide React icons matching the sidebar design.
   - Ensure the active state highlighting (`bg-sky-50 text-sky-600 font-bold`) works correctly when navigating between these pages.
   - Ensure full RTL support and responsiveness across desktop and mobile menus.